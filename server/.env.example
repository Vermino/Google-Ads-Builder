# ===========================================
# Server Configuration
# ===========================================

# Port for the API server (default: 8000)
PORT=8000

# Node environment (development, production, test)
NODE_ENV=development

# ===========================================
# CORS Configuration
# ===========================================

# Comma-separated list of allowed origins
# In production, replace with your actual frontend domain
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000

# ===========================================
# API Keys (SENSITIVE - NEVER COMMIT)
# ===========================================

# OpenAI API Key
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Claude API Key (optional)
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-claude-api-key-here

# ===========================================
# Authentication Configuration
# ===========================================

# Single API authentication token for frontend requests
# Generate a secure random token: openssl rand -hex 32
API_AUTH_TOKEN=your-secure-token-here

# Or use multiple tokens (comma-separated) for different clients
# API_AUTH_TOKENS=token1-for-prod,token2-for-dev,token3-for-staging

# ===========================================
# Rate Limiting Configuration
# ===========================================

# Time window for rate limiting (milliseconds)
# Default: 900000 (15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per time window
# Default: 100 requests per 15 minutes
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# AI Service Configuration
# ===========================================

# OpenAI Model (default: gpt-4-turbo-preview)
OPENAI_MODEL=gpt-4-turbo-preview

# Claude Model (default: claude-3-5-sonnet-20241022)
CLAUDE_MODEL=claude-3-5-sonnet-20241022

# Maximum tokens for AI responses (default: 500)
MAX_TOKENS=500

# Temperature for AI generation (0.0-1.0, default: 0.7)
TEMPERATURE=0.7

# API request timeout in milliseconds (default: 30000)
API_TIMEOUT_MS=30000

# ===========================================
# Google OAuth Configuration (for Sheets Integration)
# ===========================================

# Get these from: https://console.cloud.google.com/apis/credentials
# 1. Create a project
# 2. Enable Google Sheets API and Google Drive API
# 3. Create OAuth 2.0 credentials
# 4. Add authorized redirect URI: http://localhost:3001/api/sheets-oauth/callback

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3001/api/sheets-oauth/callback

# Frontend URL (for OAuth redirect)
CLIENT_URL=http://localhost:5173

# ===========================================
# Logging Configuration
# ===========================================

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# Enable request logging (true/false)
LOG_REQUESTS=true
