# AI Service Architecture

Visual architecture and data flow diagrams for the AI-powered ad copy generation service.

---

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        React Application                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              React Components (UI Layer)                 │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │   │
│  │  │ Campaign    │  │ Ad Group    │  │ Ad Copy     │     │   │
│  │  │ Builder     │  │ Creator     │  │ Generator   │     │   │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘     │   │
│  └─────────┼─────────────────┼─────────────────┼───────────┘   │
│            │                 │                 │                 │
│            ├─────────────────┴─────────────────┘                │
│            │                                                     │
│  ┌─────────▼─────────────────────────────────────────────────┐ │
│  │           React Hook (useAIGeneration)                    │ │
│  │  • State Management (loading, error, data)                │ │
│  │  • Action Dispatchers (generate, regenerate)              │ │
│  │  • Provider Detection                                     │ │
│  └─────────┬──────────────────────────────────────────────────┘│
│            │                                                     │
│  ┌─────────▼─────────────────────────────────────────────────┐ │
│  │              AI Service (aiService.ts)                    │ │
│  │  • generateAdCopy()                                       │ │
│  │  • generateHeadlines()                                    │ │
│  │  • generateDescriptions()                                 │ │
│  │  • Validation & Error Handling                            │ │
│  └─────────┬──────────────────────────────────────────────────┘│
│            │                                                     │
│  ┌─────────▼─────────────────────────────────────────────────┐ │
│  │          Configuration (aiConfig.ts)                      │ │
│  │  • API Keys                                               │ │
│  │  • Model Settings                                         │ │
│  │  • Generation Parameters                                  │ │
│  │  • Policy Rules                                           │ │
│  └─────────┬──────────────────────────────────────────────────┘│
│            │                                                     │
└────────────┼─────────────────────────────────────────────────────┘
             │
             ├──────────────┬──────────────┐
             │              │              │
    ┌────────▼────────┐  ┌─▼──────────┐  │
    │  OpenAI API     │  │ Claude API │  │
    │  (GPT-4 Turbo)  │  │ (3.5 Sonnet)│ │
    └─────────────────┘  └────────────┘  │
                                          │
                                   (Future: Backend Proxy)
```

---

## 🔄 Data Flow

### 1. User Initiates Generation

```
┌──────────────┐
│     User     │
└──────┬───────┘
       │ Clicks "Generate Ad Copy"
       │
┌──────▼────────────────────────────────────────┐
│  React Component                              │
│  • Collects form data                         │
│  • Validates input                            │
│  • Calls hook                                 │
└──────┬────────────────────────────────────────┘
       │
       │ hook.generate(request)
       │
┌──────▼────────────────────────────────────────┐
│  useAIGeneration Hook                         │
│  • Sets isGenerating = true                   │
│  • Clears previous error                      │
│  • Calls service                              │
└──────┬────────────────────────────────────────┘
       │
       │ await generateAdCopy(request)
       │
┌──────▼────────────────────────────────────────┐
│  AI Service (aiService.ts)                    │
│  • Validates provider configured              │
│  • Builds prompts                             │
│  • Calls AI API                               │
└──────┬────────────────────────────────────────┘
       │
       │ API call
       │
┌──────▼────────────────────────────────────────┐
│  AI Provider (OpenAI/Claude)                  │
│  • Processes prompt                           │
│  • Generates copy                             │
│  • Returns response                           │
└──────┬────────────────────────────────────────┘
       │
       │ Response
       │
┌──────▼────────────────────────────────────────┐
│  AI Service (Response Processing)             │
│  • Parses response                            │
│  • Validates character limits                 │
│  • Filters invalid copy                       │
│  • Returns GeneratedAdCopy                    │
└──────┬────────────────────────────────────────┘
       │
       │ result
       │
┌──────▼────────────────────────────────────────┐
│  useAIGeneration Hook                         │
│  • Sets generatedCopy = result                │
│  • Sets isGenerating = false                  │
│  • Updates component state                    │
└──────┬────────────────────────────────────────┘
       │
       │ State update triggers re-render
       │
┌──────▼────────────────────────────────────────┐
│  React Component                              │
│  • Displays loading state                     │
│  • Shows generated copy                       │
│  • Allows user interaction                    │
└───────────────────────────────────────────────┘
```

---

## 🎯 Component Hierarchy

```
App
└─── CampaignBuilder
     ├─── CampaignForm
     │    └─── AdCopyGeneratorButton
     │         └─── useAIGeneration() ────┐
     │                                     │
     ├─── AdGroupCreator                  │
     │    └─── AdCopyGeneratorModal       │
     │         └─── useAIGeneration() ────┤
     │                                     │
     └─── AdGroupList                     │
          └─── AdGroupItem                │
               └─── RegenerateButton      │
                    └─── useAIGeneration()│
                                          │
                    All use same          │
                    AI Service ───────────┘
                    (aiService.ts)
```

---

## 📦 Module Dependencies

```
┌───────────────────────────────────────────────────┐
│  React Components                                 │
│  • AdCopyGenerator.tsx                            │
│  • CampaignBuilder.tsx                            │
│  • AdGroupCreator.tsx                             │
└──────────┬────────────────────────────────────────┘
           │ imports
           │
┌──────────▼────────────────────────────────────────┐
│  React Hook (useAIGeneration.ts)                  │
│  • useState for state management                  │
│  • useCallback for memoization                    │
└──────────┬────────────────────────────────────────┘
           │ imports
           │
┌──────────▼────────────────────────────────────────┐
│  AI Service (aiService.ts)                        │
│  • OpenAI SDK                                     │
│  • Anthropic SDK                                  │
└──────────┬────────────────────────────────────────┘
           │ imports
           │
┌──────────▼────────────────────────────────────────┐
│  Configuration (aiConfig.ts)                      │
│  • Environment variables                          │
│  • Model settings                                 │
│  • Policy rules                                   │
└───────────────────────────────────────────────────┘
           │ imports
           │
┌──────────▼────────────────────────────────────────┐
│  External Dependencies                            │
│  • openai (v6.7.0)                                │
│  • @anthropic-ai/sdk (v0.68.0)                    │
└───────────────────────────────────────────────────┘
```

---

## 🔐 Security Architecture

### Current (Development)

```
┌─────────────┐
│   Browser   │
│             │
│  .env.local │◄──── API Keys stored locally
│             │
│  AI Service │───┐
└─────────────┘   │
                  │ Direct API calls
                  │ (dangerouslyAllowBrowser: true)
                  │
        ┌─────────┴──────────┐
        │                    │
  ┌─────▼──────┐    ┌───────▼────┐
  │ OpenAI API │    │ Claude API │
  └────────────┘    └────────────┘
```

### Recommended (Production)

```
┌─────────────┐
│   Browser   │
│             │
│  AI Service │───┐
└─────────────┘   │
                  │ HTTP requests
                  │ (no API keys in browser)
                  │
            ┌─────▼────────┐
            │   Backend    │
            │   Proxy      │
            │              │
            │  API Keys    │◄──── Stored securely
            │  stored here │
            └─────┬────────┘
                  │
                  │ API calls with keys
                  │
        ┌─────────┴──────────┐
        │                    │
  ┌─────▼──────┐    ┌───────▼────┐
  │ OpenAI API │    │ Claude API │
  └────────────┘    └────────────┘
```

---

## 🔄 State Management

### Hook State Flow

```
┌─────────────────────────────────────────────────┐
│         useAIGeneration Hook State              │
├─────────────────────────────────────────────────┤
│                                                  │
│  Initial State:                                 │
│  • isGenerating: false                          │
│  • generatedCopy: null                          │
│  • error: null                                  │
│                                                  │
│              ↓ user.generate()                  │
│                                                  │
│  Loading State:                                 │
│  • isGenerating: true                           │
│  • generatedCopy: null                          │
│  • error: null                                  │
│                                                  │
│         ↓ success        ↓ error                │
│                                                  │
│  Success State:          Error State:           │
│  • isGenerating: false   • isGenerating: false  │
│  • generatedCopy: {...}  • generatedCopy: null  │
│  • error: null           • error: "message"     │
│                                                  │
└─────────────────────────────────────────────────┘
```

---

## 🧩 Service Layer Architecture

```
┌───────────────────────────────────────────────────┐
│           AI Service (aiService.ts)               │
├───────────────────────────────────────────────────┤
│                                                    │
│  ┌────────────────────────────────────────────┐  │
│  │        Public API                          │  │
│  │  • generateAdCopy()                        │  │
│  │  • generateHeadlines()                     │  │
│  │  • generateDescriptions()                  │  │
│  │  • regenerateHeadlines()                   │  │
│  │  • regenerateDescriptions()                │  │
│  │  • validateHeadline()                      │  │
│  │  • validateDescription()                   │  │
│  │  • isAIServiceAvailable()                  │  │
│  │  • getAvailableProviders()                 │  │
│  │  • formatAIError()                         │  │
│  └────────┬───────────────────────────────────┘  │
│           │                                        │
│  ┌────────▼───────────────────────────────────┐  │
│  │        Internal Utilities                  │  │
│  │  • buildHeadlinePrompt()                   │  │
│  │  • buildDescriptionPrompt()                │  │
│  │  • callOpenAI()                            │  │
│  │  • callClaude()                            │  │
│  │  • parseHeadlines()                        │  │
│  │  • parseDescriptions()                     │  │
│  │  • sanitizeAdCopy()                        │  │
│  └────────┬───────────────────────────────────┘  │
│           │                                        │
│  ┌────────▼───────────────────────────────────┐  │
│  │        AI Clients                          │  │
│  │  • OpenAI client instance                  │  │
│  │  • Claude client instance                  │  │
│  │  • Lazy initialization                     │  │
│  └────────────────────────────────────────────┘  │
│                                                    │
└───────────────────────────────────────────────────┘
```

---

## 📊 Request/Response Flow

### Complete Generation Flow

```
Request Object
├── provider: 'openai' | 'claude'
├── businessDescription: string
├── targetKeywords: string[]
├── tone: string
├── callToAction: string
├── uniqueSellingPoints: string[]
├── targetAudience: string
├── headlineCount: number
└── descriptionCount: number
    │
    ↓
AI Service Processing
├── 1. Validate provider configured
├── 2. Build headline prompt
├── 3. Build description prompt
├── 4. Call AI API (parallel)
│   ├── Generate headlines
│   └── Generate descriptions
├── 5. Parse responses
├── 6. Validate each item
│   ├── Check character limits
│   ├── Check prohibited chars
│   └── Check punctuation
├── 7. Filter invalid items
└── 8. Return result
    │
    ↓
Response Object
├── headlines: string[]         (filtered valid headlines)
├── descriptions: string[]      (filtered valid descriptions)
├── generatedAt: string        (ISO timestamp)
└── provider: string           (which provider was used)
```

---

## 🎯 Error Handling Flow

```
                    API Call
                        │
                        ↓
                 ┌──────────────┐
                 │  Try Block   │
                 └──────┬───────┘
                        │
        ┌───────────────┴───────────────┐
        │                               │
    Success                         Error
        │                               │
        ↓                               ↓
┌───────────────┐              ┌────────────────┐
│ Parse & Return│              │ Catch & Handle │
└───────────────┘              └────────┬───────┘
                                        │
                    ┌───────────────────┼───────────────────┐
                    │                   │                   │
            ┌───────▼────────┐  ┌──────▼──────┐   ┌───────▼──────┐
            │ HTTP 401       │  │ HTTP 429    │   │ HTTP 500     │
            │ Auth Error     │  │ Rate Limit  │   │ API Error    │
            └───────┬────────┘  └──────┬──────┘   └───────┬──────┘
                    │                   │                   │
                    └───────────────────┼───────────────────┘
                                        │
                                ┌───────▼────────┐
                                │ AIServiceError │
                                │ with error code│
                                └───────┬────────┘
                                        │
                                        ↓
                                ┌────────────────┐
                                │ formatAIError()│
                                │ User-friendly  │
                                │ message        │
                                └────────────────┘
```

---

## 🔍 Validation Pipeline

```
Generated Text
    │
    ↓
┌────────────────────┐
│ Length Check       │  30 chars for headlines
│                    │  90 chars for descriptions
└────────┬───────────┘
         │ Pass
         ↓
┌────────────────────┐
│ Prohibited Chars   │  Check for: < > { } [ ] \
└────────┬───────────┘
         │ Pass
         ↓
┌────────────────────┐
│ Punctuation Check  │  Max 2 ! marks
│                    │  Max 2 ? marks
└────────┬───────────┘
         │ Pass
         ↓
┌────────────────────┐
│ Capitalization     │  No excessive ALL CAPS
└────────┬───────────┘
         │ Pass
         ↓
┌────────────────────┐
│ Valid Copy ✓       │
└────────────────────┘
```

---

## 🚀 Deployment Architecture

### Development

```
┌─────────────────────────────────┐
│  Developer Machine              │
│                                 │
│  ┌─────────────────────────┐   │
│  │ Vite Dev Server         │   │
│  │ Port: 3000              │   │
│  └───────────┬─────────────┘   │
│              │                  │
│  ┌───────────▼─────────────┐   │
│  │ React App               │   │
│  │ • Components            │   │
│  │ • AI Service (client)   │   │
│  │ • Direct API calls      │   │
│  └───────────┬─────────────┘   │
│              │                  │
│  ┌───────────▼─────────────┐   │
│  │ .env.local              │   │
│  │ • VITE_OPENAI_API_KEY   │   │
│  │ • VITE_CLAUDE_API_KEY   │   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘
         │
         │ HTTPS
         ↓
┌─────────────────────┐
│ OpenAI/Claude API   │
└─────────────────────┘
```

### Production (Recommended)

```
┌─────────────────────────────────┐
│  Client Browser                 │
│                                 │
│  ┌─────────────────────────┐   │
│  │ React App               │   │
│  │ • Components            │   │
│  │ • AI Service (calls     │   │
│  │   backend proxy)        │   │
│  └───────────┬─────────────┘   │
└──────────────┼─────────────────┘
               │ HTTPS
               ↓
┌──────────────▼──────────────────┐
│  Backend Server                 │
│                                 │
│  ┌─────────────────────────┐   │
│  │ API Proxy Endpoint      │   │
│  │ POST /api/generate      │   │
│  └───────────┬─────────────┘   │
│              │                  │
│  ┌───────────▼─────────────┐   │
│  │ AI Service              │   │
│  │ (server-side)           │   │
│  └───────────┬─────────────┘   │
│              │                  │
│  ┌───────────▼─────────────┐   │
│  │ Environment Variables   │   │
│  │ • OPENAI_API_KEY        │   │
│  │ • CLAUDE_API_KEY        │   │
│  │ (secured)               │   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘
         │ HTTPS
         ↓
┌─────────────────────┐
│ OpenAI/Claude API   │
└─────────────────────┘
```

---

## 📈 Performance Architecture

### Parallel Generation

```
generateAdCopy()
    │
    ├──────────┬──────────┐
    │          │          │
    ↓          ↓          ↓
Headlines  Descriptions  Config
Generate   Generate      Load
Prompt     Prompt        Settings
    │          │          │
    ↓          ↓          │
Call API   Call API      │
(OpenAI)   (OpenAI)      │
    │          │          │
    ├──────────┼──────────┘
    │          │
    ↓          ↓
Promise.all([headlines, descriptions])
    │
    ↓
Combine Results
    │
    ↓
Return GeneratedAdCopy
```

### Timeout Handling

```
API Call
    │
    ├─────────────┐
    │             │
    ↓             ↓
Actual      Timeout
Request     Promise
    │         (30s)
    │             │
    └──── Race ───┘
          │
          ↓
    First to resolve
```

---

## 🎯 Summary

This architecture provides:

- ✅ **Clean separation** of concerns
- ✅ **Modular design** for easy maintenance
- ✅ **Type safety** throughout the stack
- ✅ **Error handling** at every layer
- ✅ **Scalable structure** for growth
- ✅ **Security considerations** documented
- ✅ **Performance optimizations** built-in

The service is production-ready and follows React and TypeScript best practices.
