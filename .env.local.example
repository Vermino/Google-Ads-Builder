# Backend API Configuration
# Copy this file to .env.local and update with your values

# Backend API URL
# Default: http://localhost:3001
VITE_API_URL=http://localhost:3001

# Backend API Authentication Token
# Get this from your administrator or generate it on the backend
VITE_API_TOKEN=your-api-token-here

# Note: API keys for OpenAI and Claude are now stored on the backend server
# They are no longer needed in the frontend .env.local file
